# Autor: David Assef
# Descrição: Exemplo de variáveis de ambiente para o frontend (Vite)
# Data: 07-09-2025
# MIT License

# Copie este arquivo para .env.local e preencha com os valores do seu projeto Supabase.
# Em provedores de deploy (Netlify/Vercel), defina as variáveis diretamente nas configurações do projeto.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# O cliente REST usa base relativa por padrão (proxy em dev). Ajuste se necessário.
# VITE_API_BASE_URL=/api/v1
